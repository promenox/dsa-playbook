-----------------------------------------
TYPICAL WORKFLOW (Java)
-----------------------------------------

1. Clean previous builds:
   $ make clean

2. Compile the bytecode into the build directory:
   $ make build

3. Run the program:
   $ make run

4. Rebuild and run in one step:
   $ make clean run
